[package]
name = "aetherc"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Command-line driver for the Aether compiler platform."

# The default binary target (`src/main.rs`) is named after the package. Keeping
# it dependency-free for now is deliberate; see TECH_DEBT.md for the planned
# migration to `clap` once the CLI surface grows.

[lints]
workspace = true
