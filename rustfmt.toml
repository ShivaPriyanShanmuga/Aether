# Formatting is not negotiable: `cargo fmt` is the single source of truth and CI
# verifies it. Only stable rustfmt options are configured here so `cargo fmt`
# works on the pinned stable toolchain without requiring nightly.
edition = "2024"
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
